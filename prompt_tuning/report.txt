Prompt Tuning学号： 3220100975姓名： 汪珉凯1. Project Introduction1.1. 选题本实验旨 在探索提示学习（Prompt Learning）在?然语?处理任务中的应?，具体实现硬提示学习（Hard Prompt Tuning）和软提示学习（Soft Prompt Tuning）两种?法，并在情感分类任务中进?效果对?。1.2. ?作简介1.2.1 执?GLUE数据集上的软提示训练代码通过MindSpore框架运?软提示代码完成训练，并进?评估。1.2.2 在情感分类任务上实现硬提示与软提示训练通过MindSpore框架实现硬提示和软提示学习?法，完成情感分类任务（?分类：   正?/负?）。具体包括：  数据预处理：  对情感分类数据集（rt-polarity.meg和rt-polarity.pos）进?清理、分词、索引化和张量化。 硬提示学习： 设计固定的?然语?模板（如“这个句?的情感是: ”）作为输?提示，训练模型。   软提示学习：  通过可学习的嵌?向量（soft prompts）作为输?提示，利?PEFT库实现微调。  测试与评估： ?较两种?法在情感分类任务中的表现差异。1.3. 开发环境及系统运?要求1.3.1. 开发?具 MindSpore框架  Python Jupyter Notebook1.3.2. 开发包 mindspore  mindnlp numpy  tqdm evaluate2. Technical Details2.1. 理论知识
 . 提示学习（Prompt Learning）  核?思想：  通过设计输?提示（Prompt）引导预训练语?模型（PLM）?成特定任务的输出，减少对模型参数的修改。 数学表达： . 硬提示学习（Hard Prompt Tuning）特点： 使?固定的?然语?模板（如“情感是正?的吗？ ”）作为提示。示例： 原始输?： [CLS] 电影剧情紧凑，?常精彩。 [SEP]  添加硬提示后： [CLS] 情感是正?的吗？电影剧情紧凑，?常精彩。  [SEP]  局限性： 依赖??设计提示，泛化能?较弱。 . 软提示学习（Soft Prompt Tuning）  特点： 在输?嵌?层前添加可训练的连续向量（soft prompts），通过梯度下降优化。  示例：  优势： ?需??设计提示，?动适应任务需求，参数效率?。2.2. 具体算法本实验主要基于RoBERTa-large模型，结合软提示（Soft Prompt）和硬提示（Hard Prompt）两种?法进??本分类任务（情感分析）。以下是两种提示?法的具体算法：软提示（Soft Prompt） . 模型架构：  在RoBERTa-large模型的基础上，通过PEFT（Parameter-Efficient Fine-Tuning）库添加可训练的虚拟令牌（Virtual Tokens）。这些虚拟令牌的嵌?向量在训练过程中会被优化，?RoBERTa的主?参数保持冻结。 . 训练过程： 输??本通过RoBERTa的Tokenizer处理，?成input_ids和attention_mask。  虚拟令牌的嵌?向量与输??本的嵌?向量拼接，形成新的输?表示。  模型基于拼接后的输?进?前向传播，计算分类损失。  通过反向传播优化虚拟令牌的嵌?向量和分类头的参数。 . 优化?标：  最?化交叉熵损失函数，提升模型在验证集上的准确率和F1分数。
硬提示（Hard Prompt） . 提示设计： 在输??本前添加固定的提示前缀（如“This review expresses a positive sentiment. The review is: ”），直接引导模型关注情感分类任务。 . 训练过程： 输??本与硬提示拼接后，通过Tokenizer处理?成input_ids和attention_mask。  模型基于拼接后的输?进?前向传播，计算分类损失。   通过反向传播优化RoBERTa的所有参数（全参数微调）。  . 优化?标：  同样以交叉熵损失函数为?标，通过硬提示显式引导模型学习任务相关特征。2.3. 技术细节（重要函数）2.3.1. 来?于库的重要函数  . AutoTokenizer.from_pretrained  功能：  加载预训练模型的Tokenizer，?持动态填充?向（padding_side）和镜像源  （mirror）。  关键参数：  padding_side：  指定填充?向（left或right），确保与模型架构兼容。 mirror： 指定模型下载镜像源（如modelscope）。   .  AutoModelForSequenceClassification.from_pretrained 功能： 加载预训练的分类模型，?持返回字典格式的输出。  关键参数：  return_dict=True：  确保输出为字典格式，便于后续处理。 . get_peft_model功能： 将基础模型转换为PEFT模型，添加可训练的提示参数。关键配置：指定任务类型为序列分类，并设置虚拟令牌数量为10。 . AdamW 功能： 实现?适应矩估计优化器，?持权重衰减。  关键参数：  params=model.trainable_params()： 仅优化可训练参数（如虚拟令牌和分类头）。  . get_linear_schedule_with_warmup功能： ?成带预热的学习率调度器，线性调整学习率。关键参数：  num_warmup_steps：  预热步数，通常设置为总训练步数的6%。
2.3.2. ??编写的重要函数 . MovieReview类  功能： 处理影评数据集，?持数据读取、清洗、分割和向量化。  关键?法：  read_data：  读取?件内容，清洗?本（去除标点、数字等），并根据?件类型（.pos/.neg）添加硬提示。  split_dataset：  按?例分割训练集和测试集，确保数据分布均衡。 . Generator类  功能： 将数据列表转换为MindSpore的GeneratorDataset兼容格式。  关键?法： 	getitem	： 返回数据、标签和索引的三元组，?持动态批处理。 . MapFunc类功能： 将原始?本映射为模型输?（input_ids和attention_mask）。关键逻辑： . forward_fn与训练循环功能： 定义前向传播和训练流程。关键步骤：  使?grad_fn计算损失和梯度。 调?optimizer.step和lr_scheduler.step更新参数和学习率。  在验证集上计算准确率和F1分数：3. Experiment Results3.1. 执?GLUE数据集上的软提示训练代码上传roberta_sequence_classification.ipynb?件。环境设置与依赖安装   安装MindNLP、MindSpore和transformers等必要库
  设置Hugging Face镜像端点模型与数据集加载  加载Roberta-large模型和tokenizer 加载GLUE-MRPC数据集并进?预处理可以看到，打印出第?批次第数据，符合预期：Prompt Tuning配置  设置Prompt Tuning参数： 选择任务类型为序列分类，学习率为0.001
    创建Peft模型（参数?效微调模型）模型训练使?AdamW优化器和线性学习率调度器打印参与微调的模型参数如下： 定义训练逻辑   评估指标计算（准确率和F1分数） ?先定义前向计算和梯度计算函数，然后在每个训练周期中遍历训练数据集计算损失和梯度并?优化器更新参数，同时使?学习率调度器调整学习率；在评估阶段，模型切换到评估模式并在验证集上计算预测结果和评估指标（如准确率）。整个过程通过进度条显示迭代进度，并定期输出模型在验证集上的性能指标，?于监控训练效果。3.2 在情感分类任务上实现软提示训练安装并导?相关依赖库，主要包括mindspore,   mindnlp等，以及后者?带的peft包。
配置超参数以及peft可以看到，我将batch??设置为64，学习率设置为1e-3，epochs设置为10，以及?系列其他参数，并规定环境为Ascend。定义tokenizer，?于数据格式转化。后?根据TextCNN等内容进?数据处理：定义Generator类类功能：    ?于封装数据集，提供索引访问和?度计算的功能。主要?法： 	getitem	(self, item)：  根据索引返回?个数据样本，包括数据、标签和索引。    	len	(self)： 返回数据集的?度。定义MovieRiview类
 定义read_data函数 定义train_dataset函数类功能：     ?于加载、预处理和分割影评数据集，并提供训练和测试数据集的?成?法。主要?法和属性：	init	(self, root_dir, split)：  初始化类，加载影评数据集。  读取影评?件（正?影评和负?影评），并统计句??度等信息。  将影评数据分为正?（Pos）和负?（Neg）两类。  根据 split 参数划分训练集和测试集。read_data(self, filePath)：   读取影评?件并进?简单的?本清理（去除标点符号、数字等）。  将清理后的句?和对应的标签（正?或负?）存储到 Pos 或 Neg 列表中。text2vec(self, maxlen)：  将句?转换为固定?度的向量表示。
   构建词汇表（Vocab），将每个单词映射为?个唯?的索引。 split_dataset(self, split)： 根据 split 参数将数据集划分为训练集和测试集。  使?随机打乱的?式确保数据分布均匀。 get_dict_len(self)：  返回词汇表的?度。 create_train_dataset(self) 和 create_test_dataset(self)：          使? GeneratorDataset 创建 MindSpore 数据集对象，分别?于训练和测试。定义MapFunc类定义get_dataset  函数，?于将数据集转换为批量化的模型输?。加载影评数据集，并?成训练和测试数据集。结果如下：加载预训练的 RoBERTa 模型，并应? PEFT 配置
   定义优化器为AdamW,学习策略为包含warmup阶段的线性学习率调整策略。  按照如下步骤定义训练逻辑： . 构建正向计算函数 . 函数变换，获取微分函数 . 定义训练?个step的逻辑  .  遍历训练数据集进?模型训练，同时每?个epoch后，遍历验证数据集获取当前的评价指标（accuracy、f1  score） （具体和3.1部分?致，此处不再赘述）3.2.1 情感分类软提示训练结果
可以看到，随着训练轮数的上升，准确率逐步提升到80%   左右趋于稳定，?f1分数则是趋于0.8达到稳定。3.3 在情感分类任务上实现硬提示训练基本与软提示的训练相同，但是有以下?点不同：新增hard_prompt定义：修改MovieReview类的read_data函数可以看到：  根据file名，在训练数据前加上固定的提示词。
3.3.1 情感分类硬提示训练结果可以看到，使?硬提示训练后，准确率也能稳步提升。